// Высота платформы на которой располагаются иконки героя и премятствий
const groundHeight = 70;

// Количество очков при старте игры по умолчанию
const defaultScore = 0;

// Размеры иконки героя
const heroHeight = 50;
const heroWidth = 50;

// Размеры иконки монетки
const coinHeight = 50;
const coinWidth = 50;

// Размеры иконки препятствия
const obstacleHeight = 50;
const obstacleWidth = 50;

// На сколько увеличивается счёт за 1 монету
const coinValue = 50;

export {
    groundHeight,
    defaultScore,
    heroHeight,
    heroWidth,
    coinHeight,
    coinWidth,
    obstacleHeight,
    obstacleWidth,
    coinValue,
};
